import React from 'react';
import { action } from '@storybook/addon-actions';
import QuestionForm from '../Components/QuestionForm';

export default {
    title: 'QuestionForm',
    component: QuestionForm
};

export const Text = () => <QuestionForm />;

